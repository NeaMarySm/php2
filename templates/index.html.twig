<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    {% block title %}
        <title>{{title}}</title>
    {% endblock %} 
    {% block style %}
     <style type='text/css'>
        body {
            background-color: black;
        }
        a {
            text-decoration: none;
        }
        .gallery-container {
            display: flex;
            justify-content: flex-start;
            flex-wrap: wrap;
        }
        .gallery-item {
            margin: 10px;
            padding: 0;
        }
        .gallery-item:hover {
            box-shadow: 0px 0px 16px 10px rgba(239, 242, 244, 0.88);
        }
        .img-container {
            display: flex;
            justify-content: center;
        }
        .view_text {
            text-align: center;
            color: #fff;
        }
        .btn {
            border: 1px solid #fff;
            color: #fff;
            padding: 5px;
            float: right;
            margin-left: 30px;
        }
        .btn:hover {
            box-shadow: 0px 0px 16px 10px rgba(239, 242, 244, 0.88);
        }
    </style>
    {% endblock %}
   
</head>
{% block body %}
    <body>
        {% for image in images %}
        <a href='index2.php?id={{image.id}}' target='_blank' >
            <img src='{{dir}}/{{image.file}}' alt='' width='150px' height='120px' class='gallery-item'>
        </a>
        {% endfor %}
    </body>
{% endblock %}

</html>